fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
pydantic==2.13.4
pydantic-settings==2.15.0
jinja2==3.1.6
httpx==0.28.1
python-dotenv==1.1.1

# Desktop shell. Renders through the WebView2 runtime built into Windows, so
# there is no bundled browser.
pywebview==6.2.1

# Tray icon, so closing the window keeps the recorder running.
pystray==0.19.5
pillow==12.1.0
PyAudioWPatch>=0.2.12
